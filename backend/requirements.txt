fastapi==0.136.1
uvicorn==0.47.0
sqlalchemy==2.0.51
pydantic==2.12.5
email-validator==2.3.0
python-dotenv==1.0.1

# PostgreSQL adapter — required when DATABASE_URL points at PostgreSQL/Supabase.
# For SQLite (default local dev) this is not needed — SQLite is built into Python.
psycopg2-binary==2.9.12
